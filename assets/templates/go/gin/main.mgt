package main

import (
    "log"

    "github.com/gin-gonic/gin"
)

{{>partial_header}}

func main() {
    r := gin.Default()

    // Register routes
    RegisterRoutes(r)

    if err := r.Run(":80"); err != nil {
        log.Fatal("Server run failed:", err)
    }
}
